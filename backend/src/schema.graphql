
type User {
  id: ID!
  name: String!
  email: String!
}

type Query {
users: [User!]!
user(id: ID!): User
}

type Mutation {
createUser(name: String!, email: String!): User
editUser(name: String!, email: String!): User

}

type Subscription{
  usersRT: [User!]!

}


